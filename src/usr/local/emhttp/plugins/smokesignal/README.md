**SmokeSignal**

Pre-reboot health check for Unraid: a single GO / CAUTION / NO-GO verdict before you reboot, so you never reboot into a known landmine.

Advisory only &mdash; it reads the host and reports, it never changes anything. Adds a button next to Reboot on the Main tab and an icon on the Dashboard.
